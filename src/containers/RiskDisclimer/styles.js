import {StyleSheet} from 'react-native';

import {Metrics, Colors} from '../../theme';

export default StyleSheet.create({
  container: {
    backgroundColor: '#000',
    flex: 1,
    padding: 20,
  },
  disclimerContainer: {backgroundColor: '#999', padding: 20},
  disclimerText: {color: '#fff', fontSize: 20},
});
